{% extends 'base.html.twig' %}

{% block title %}List enseignant
{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('bower_components/datatables.net-bs/css/dataTables.bootstrap.min.css') }}">
{% endblock %}


{% block insideHeader %}
    <section class="content-header">
        <h1>
            Gestions des accès enseignant
            {#<small>{{ parcour.type }}</small>#}
        </h1>
        <ol class="breadcrumb">
            <li>
                <a href="#">
                    <i class="fa fa-dashboard"></i>
                    Gestions des accès enseignant</a>
            </li>
            {#<li class="active">{{ parcour.type }}</li>#}
        </ol>
    </section>
{% endblock %}

{% block body %}
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">Gestions des accès enseignant</h3>
        </div>
        <!-- /.box-header -->
        <div class="box-body">
            <table class="table table-bordered table-striped" id="example1">
                <thead>
                    <tr>
                        <th>Pseudo</th>
                        <th>Nom</th>
                        <th>Prénom</th>
                        <th>Type</th>
                        <th>Statut</th>
                        <th>Rôle</th>
                    </tr>
                </thead>
                <tbody>
                {% for et in enseignant %}
                    <tr>
                        {% if et.login != null %}
                            <td>{{ et.login.username }}</td>
                        {% else %}
                            <td>Pas d'accès</td>
                        {% endif %}
                        <td>{{ et.nom }}</td>
                        <td>{{ et.prenom }}</td>
                        <td>{{ et.type.type }}</td>
                        {% if et.login != null %}
                            <td>{{et.login.isActive}}</td>
                        {% else %}
                            <td>Pas d'accès</td>
                        {% endif %}
                        {% if et.login != null %}
                            <td>{{et.login.roles}}</td>
                        {% else %}
                            <td>Pas d'accès</td>
                        {% endif %}
                    </tr>
                {% endfor %}
                </tbody>
                <tfoot>
                    <tr>
                        <th>Pseudo</th>
                        <th>Nom</th>
                        <th>Prénom</th>
                        <th>Parcours</th>
                        <th>Date d'entré</th>
                    </tr>
                </tfoot>
            </table>
        </div>
        <!-- /.box-body -->
    </div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('bower_components/jquery/dist/jquery.min.js') }}"></script>
    <script src="{{ asset('bower_components/bootstrap/dist/js/bootstrap.min.js') }}"></script>
    <script src="{{ asset('dist/js/adminlte.min.js') }}"></script>
    <script src="{{ asset('bower_components/datatables.net/js/jquery.dataTables.js') }}"></script>
    <script src="{{ asset('bower_components/datatables.net-bs/js/dataTables.bootstrap.min.js') }}"></script>
    <script src="{{ asset('bower_components/jquery-slimscroll/jquery.slimscroll.min.js') }}"></script>
    <script src="{{ asset('bower_components/fastclick/lib/fastclick.js') }}"></script>
    <script>
        $('#example1').DataTable()
    </script>
{% endblock %}
