{{ form_start(form,{'action': path('search_result'), 'method': 'POST','attr':{'class' : 'sidebar-form'}}) }}
    <div class="input-group">
        {{ form_row(form.search,{'attr':{'class' : 'form-control' , 'placeholder':'Rechercher...'}}) }}
        <span class="input-group-btn">
            <button class="btn btn-flat" id="search-btn" name="search" type="submit" style="position: relative;margin-bottom: 0px;">
                <i class="fa fa-search"></i>
            </button>
        </span>
    </div>
{{ form_end(form) }}

